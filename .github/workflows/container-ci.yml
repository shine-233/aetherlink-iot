name: Container build

on:
  pull_request:
  push:
    branches:
      - main
    tags:
      - 'v*.*.*'
  workflow_dispatch:

# This is a build-only gate. It must never publish a PR or main image.
permissions:
  contents: read

concurrency:
  group: container-build-${{ github.ref }}
  cancel-in-progress: true

jobs:
  build:
    name: Container build ${{ matrix.image }}
    runs-on: ubuntu-latest
    timeout-minutes: 35
    strategy:
      fail-fast: false
      matrix:
        include:
          - image: aetherlink-iot-backend
            context: ./backend
            file: ./backend/Dockerfile
          - image: aetherlink-iot-frontend
            context: ./frontend
            file: ./frontend/Dockerfile
          - image: aetherlink-iot-mqtt-broker
            context: ./mqtt-broker
            file: ./mqtt-broker/Dockerfile
    steps:
      - name: Check out repository
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@f87e5991a6d7451dcb8d9637bfbc97413f497069 # v4.4.1

      - name: Build image without publishing
        uses: docker/build-push-action@c3c9e263c25d99ce0380d002d59b67737d91b0dc # v7.4.0
        with:
          context: ${{ matrix.context }}
          file: ${{ matrix.file }}
          platforms: linux/amd64
          tags: local/${{ matrix.image }}:ci-${{ github.sha }}
          push: false
          provenance: false
          sbom: false
