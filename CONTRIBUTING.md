# 贡献指南

感谢你改进 AetherLink IoT。本仓库正在按公开 GitHub 协作流程整理，提交应尽量做到容易审查、容易复现、容易验证。

## 工作规则

- 保持 `frontend/`、`backend/`、`mqtt-broker/`、`automation_tests/` 的职责边界清晰。
- 如果改动会影响模块归属、运行行为、公开 API 形态或验证证据，请同步更新对应目录的 README。
- 在重构大型集成文件或服务层业务逻辑前，优先补充聚焦测试，避免只做结构改动却没有行为保护。
- 生成文件的保留、删除和再生成规则以 `GENERATED_FILES.md` 为准。
- 不要把本地凭据、运行报告、构建产物、依赖目录、浏览器认证状态或环境专用配置提交到 Git。

## 验证要求

发布级验证门槛见 `VALIDATION.md`。较小改动应运行能够证明被触及行为的最窄检查，并在评审说明中记录哪些检查没有运行、为什么没有运行。

不要把历史归档、旧报告或局部静态检查写成“当前版本已通过完整发布验证”。如果验证证据不是从当前工作树重新生成的，请明确标注为历史参考或局部证据。

## 文档要求

新增源码文件应包含简短的文件级或包级说明，说明用途、关键输入、主要副作用和维护注意事项。目录 README 应说明该目录的职责、重要文件、上下游关系，以及后续审查或重构建议。

## 维护与审查建议

- 评审时优先检查改动是否越过了模块边界，是否需要同步更新 README、验证说明或生成文件清单。
- 如果一次提交同时修改业务逻辑和大规模格式/注释，建议拆分，方便回溯风险。
- 发布前请再次确认 `PUBLICATION.md`、`SECURITY.md`、`VALIDATION.md` 与实际提交范围一致。
