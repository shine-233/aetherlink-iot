# RBAC 按角色收紧 · 迭代手册（产品节奏 Runbook）

> 版本：2026-09-05 · 依据：63.sql（状态 quo 显式化）+ 64.sql（第一批收紧）及其运行期验证。
> 目的：把"按角色收紧"从一次性工程动作固化为**可复制的迭代节奏**——每批一个小迁移、一套验证协议、一个提交。

---

## 一、当前状态（2026-09-05，sys_version=65）

| 角色 | p 行数 | 语义 |
|---|---|---|
| SYS_ADMIN | 287 | 全量（平台超级管理员，不收紧） |
| TENANT_ADMIN | 275 | 租户内全量 + 撤平台面（插件注册/系统功能/平台监控/平台字典写，64.sql） |
| TENANT_USER | 255 | 业务面 + 撤全部管理面（64.sql：用户/角色/权限/SSO/OpenAPI/字典写/UI 管理/审计/监控等 32 行） |

收紧紧的是**越权面**，不触碰业务面：device 77 / telemetry 21 / command 15 / board 13 / alarm 13 等
对 TENANT_USER 是合理保留（一线操作员的日常工作面）。

## 二、迭代节奏（每批怎么做）

**一批 = 一个决策 + 一个迁移 + 一套验证 + 一个提交**，预计节奏为"每产品迭代一批"（而非一次性到底），
原因是每批收紧都会改变对应角色的控制台可见行为，需要产品侧同步确认菜单/入口文案。

1. **决策**：从"三、候选清单"取一批（≤15 行/批，保证评审注意力）；产品确认每条的角色归属。
2. **迁移**：新建 `sql/{下一版本}.sql`，沿用 64.sql 模式：
   ```sql
   DELETE FROM casbin_rule
   WHERE ptype = 'p' AND v0 = '{角色}' AND v1 IN ( '{api/v1/...}' , ... );
   ```
   头注释写明：批次目的、逐条理由、前端入口影响。DELETE 天然幂等。
   同步 `backend/pkg/global/global.go` 的 `VERSION_NUMBER` +1。
3. **验证协议**（缺一不可，全部要当次运行证据）：
   - **权限矩阵**：scratch 库三角色登录，撤除端点断言 403/404、保留端点断言 200（参照 64.sql 的 32/32 矩阵做法）；
   - **automation 全量**：`set -a; . automation_tests/.env.local; set +a; export API_BASE_URL=... HEALTH_URL=...` 后 `npx mocha tests/*.test.js`，0 真实失败（429 为限流按设计，窗口重置重跑）；
   - **C5 门禁**：`python c5_validate.py` 28/28（主栈，验证迁移已应用后）。
4. **提交**：迁移 + VERSION_NUMBER + ROADMAP 行 + 本手册的候选清单勾选，一个 commit。

## 三、候选清单（TENANT_USER 剩余 255 行中的下一批，按风险排序）

| 候选 | 现有行数 | 建议 | 理由 |
|---|---|---|---|
| `rule-chains` 写操作 | 3 | **收紧 → TENANT_ADMIN** | 规则链是租户级编排资产，普通用户只应消费结果 |
| `data_script` 写操作 | 4 | **收紧 → TENANT_ADMIN** | 数据处理脚本影响全租户遥测加工 |
| `entity_versions` 快照/恢复 | 3 | **收紧 → TENANT_ADMIN** | 恢复操作可回滚他人工作区，属管理动作 |
| `calculated_fields` 写操作 | 3 | **收紧 → TENANT_ADMIN** | 计算字段定义影响面同 data_script |
| `ota` 升级包/任务写 | 8 | 分裂：读保留，**写收紧** | OTA 推送影响全量设备，读进度合理 |
| `notification` 配置写 | 7 | 分裂：读/订阅保留，**配置写收紧** | 通知渠道配置是管理员职责 |
| `device_config` 写 | 8 | 产品决策 | 若一线用户需自建直连配置则保留；默认建议收紧 |
| `asset` 写 | 4 | 产品决策 | 资产树结构调整通常归管理员，挂载/查看保留 |

**判断框架**（决策时逐条套用）：
- 影响面是"自己可见"还是"全租户可见"？→ 全租户 → ADMIN
- 操作可逆吗？恢复/回滚类 → ADMIN
- 是"日常操作"还是"一次性的配置动作"？→ 配置动作 → ADMIN
- 收紧后控制台对应页面是否仍可达（菜单过滤 `/ui_elements/menu` 已按角色过滤）？

## 四、TENANT_ADMIN 的下一批（平台面残余）

64.sql 已撤插件注册/系统功能/平台监控/平台字典写。剩余候选：
- `open/keys`（OpenAPI 密钥）：若定位为"平台颁发"则收紧 → SYS_ADMIN；若租户自管则保留（当前保留）。
- `tenant` 管理端点：SYS_ADMIN 专属，确认无泄漏即可。

## 五、回滚

- 迁移前 p 行即"收紧前状态"，回滚 = 把该批 DELETE 的 (v0,v1) 对重新 INSERT（从 git 历史的
  64.sql/批次文件反推，或从备份库导出）。
- 建议每批收紧前 `pg_dump --table=casbin_rule` 留档（一条命令，成本极低）。

## 六、历史批次

| 批次 | 迁移 | 内容 | 验证 |
|---|---|---|---|
| 2026-09-05 | 64.sql | TU 撤管理面 32 行；TA 撤平台面 12 行 | 32/32 矩阵 + automation 677P/0 真实失败 + C5 28/28 |
