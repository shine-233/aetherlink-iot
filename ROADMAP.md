# AetherLink IoT 路线图（Roadmap）

本文件是公开的能力规划入口，只描述"计划中的方向"，不包含任何客户信息、项目条款或内部进度台账。

## 当前状态

- 源码、离线门禁与发布流程的现状以 [VALIDATION.md](./VALIDATION.md) 与 [PUBLICATION.md](./PUBLICATION.md) 为准。
- 真实 RDI 设备、目标部署、HTTPS/TLS、公网 MQTT 与 backup/restore 仍处于 `not-tested` / `pending` / `configuration-required` 状态。

## 计划中的能力方向

以下条目是当前最明确、最值得投入的后续能力，按优先级大致排序：

1. **设备状态镜像 / twin-lite**
   - 设备期望态与上报态的轻量镜像模型。
   - 期望值下发、偏差检测与回读闭环。

2. **Fleet 批量治理**
   - 面向设备分组、标签和条件的索引化筛选。
   - 跨大规模设备集合的批量配置、批量命令与治理动作。

3. **自动化场景可靠性**
   - 场景预验证（保存前 dry-run）。
   - 执行轨迹留存与失败诊断视图。

4. **OTA 治理增强**
   - 目标设备预览与合规约束。
   - 升级回滚可见性与批量发布治理。

5. **验证证据闭环**
   - 把 API 自动化、E2E、负例与环境预检串成可重复的稳定流水线。
   - CI 中自举完整 Compose 栈并归档运行时证据。

## 已明确的非目标

- 不在平台侧判定固件行为边界（如按键时长语义），平台只承接固件信令。
- ThingsVis 兼容集成保持显式可选，不进入默认核心栈。

## 维护约定

- 本文件只保留"当前计划"；已完成能力的说明见各模块 README 与 [CODE_WIKI.md](./CODE_WIKI.md)。
- 历史决策记录保存在维护者私有环境中，不随公开仓库分发。
