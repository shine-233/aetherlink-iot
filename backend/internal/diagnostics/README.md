# 后端诊断辅助层

`backend/internal/diagnostics` 保存排障辅助逻辑和运行诊断支持能力，用于帮助定位问题，而不是承载主业务流程。

## 目录职责

- 提供运行诊断、问题定位和辅助排障所需的内部能力。
- 支持维护者快速收集上下文，但不应泄露不该公开的内部状态。

## 审查与重构建议

- 问题：诊断输出如果边界不清，可能把敏感状态直接暴露到日志或对外接口。
- 改进方案：明确哪些字段可公开、哪些只能内部排障使用，并在输出前做必要脱敏。
- 预期效果：排障更高效，同时降低泄露敏感信息的风险。
