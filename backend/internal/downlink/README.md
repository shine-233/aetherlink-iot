# 后端下行链路

`backend/internal/downlink` 负责后端向设备侧下发命令或控制消息的后端协作路径。

## 目录职责

- 组织设备命令、控制消息或相关下行语义的后端处理。
- 协调业务层与 MQTT/Broker/设备侧之间的消息形态和发送边界。

## 审查与重构建议

- 问题：下行链路改动会直接影响设备命令投递、Broker 预期和协议兼容。
- 改进方案：继续明确消息结构、发送时机和错误传播边界，后续统一进入更强验证清单。
- 预期效果：设备命令路径更安全，协议改动影响面更容易评估。
