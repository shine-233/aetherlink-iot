// 文件用途：说明 tptodb 兼容客户端包的外部协议边界。
// 核心逻辑：该包保留 tp_to_db gRPC 生成协议的 wire name，并向后端暴露较中性的可选遥测查询客户端封装。
// 关键注意事项：默认本地存储不会初始化本客户端；不要手工改 generated *.pb.go 里的服务符号，真正改名必须从 .proto 源文件重新生成并制定兼容计划。
// 重构建议：建议补齐 proto 源文件来源、生成命令和兼容矩阵，避免后续维护者误把生成代码当作普通业务代码修改。
package tptodb
