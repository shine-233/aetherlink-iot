# P0.1 发布预检门禁 —— 证据记录

日期：2026-09-11
环境：Windows / Windows PowerShell 5.1
对象：`scripts/roadmap/preflight-release.ps1`（原为空壳，已填实）

## 结论

| 项 | 状态 | 依据 |
| --- | --- | --- |
| 静态不变量（迁移上界一致性、打包文档在位） | **有运行期证据** | 对真实仓库实跑，两项 PASS |
| 失败路径（缺文件即阻断） | **有运行期证据** | 对空目录实跑，2 项 FAIL + VERDICT=BLOCKED，退出码 1 |
| 缺环境时的行为 | **有运行期证据** | 无目标时输出 PENDING 并退出码 2，**不伪装为通过** |
| 工作树状态检查 | **PENDING** | 执行环境中 PowerShell 无 `git` 命令，脚本按契约报 PENDING 而非崩溃 |
| 目标服务器 HTTPS/TLS、MQTTS、公网 MQTT 验收 | **未做** | 需要真实部署环境，脚本按契约报 PENDING |
| `validate-deploy.ps1`（部署校验） | **有运行期证据** | 三个场景实跑：无目标→退出码 2；http 目标→BLOCKED 退出码 1；https 目标→退出码 2 |
| `backup-restore.ps1` | **仍为空壳** | 需真实备份工具与目标库，本轮未做 |

**P0.1 仍记 pending/partial，不宣称 done。**

## 实现要点

- **迁移上界一致性**：比对 `backend/pkg/global/global.go` 的 `VERSION_NUMBER` 与
  `backend/sql/` 下最大编号迁移。这是 AGENTS.md 明令要求、且不需要数据库即可验证的不变量。
- **打包文档在位**：`deploy/package.sh|ps1` 按文件名打包 `README.md`、`START-HERE.md`、
  `VALIDATION.md`、`SECURITY.md`、`THIRD_PARTY_NOTICES.md`，缺一份即部署包不完整。
- **工作树状态**：脏树只 WARN 不阻断（避免共享开发树恒定失败），但必须显式标注快照未证明。
- **环境探针**：无目标 / 不可达一律 PENDING（退出码 2）。**绝不把"没验证"写成"通过"**。

## 实跑结果

### 正向（对真实仓库）

```
AetherLink release preflight :: RepoRoot=C:\Users\Zz\Documents\projects\active\aetherlink-iot CheckOnly=True
[PASS] migration-ceiling :: VERSION_NUMBER=88 matches max migration=88
[PASS] release-docs :: packaging docs present: README.md, START-HERE.md, VALIDATION.md, SECURITY.md, THIRD_PARTY_NOTICES.md
[PENDING] worktree-state :: git not available in this session; snapshot unproven
[PENDING] api-reachable :: no target supplied; live check not executed
[PENDING] frontend-reachable :: no target supplied; live check not executed

VERDICT=PENDING :: static invariants pass, 3 live check(s) not executed
EXITCODE=2
```

注：`VERSION_NUMBER=88` 为执行时刻的真实值，脚本每次运行都会重新读取，不写死。

### 负向（对空目录，验证失败路径真的会阻断）

```
AetherLink release preflight :: RepoRoot=<empty dir> CheckOnly=True
[FAIL] migration-ceiling :: missing <empty dir>\backend\pkg\global\global.go
[FAIL] release-docs :: missing: README.md, START-HERE.md, VALIDATION.md, SECURITY.md, THIRD_PARTY_NOTICES.md
[PENDING] worktree-state :: git not available in this session; snapshot unproven
[PENDING] api-reachable :: no target supplied; live check not executed
[PENDING] frontend-reachable :: no target supplied; live check not executed

VERDICT=BLOCKED :: 2 static invariant(s) failed
EXITCODE=1
```

## validate-deploy.ps1 实跑（三个场景）

### 无目标

```
[PENDING] target-present :: AETHERLINK_BASE_URL not supplied; nothing to validate
[PENDING] mqtt-reachable :: AETHERLINK_MQTT_ACCESS_ADDRESS not supplied
VERDICT=PENDING :: 2 check(s) not executed; deployment unproven
EXITCODE=2
```

### http 目标（应被策略阻断）

```
[PASS] target-present :: http://203.0.113.10:8080
[FAIL] https-scheme :: server target must use https, got http
[PASS] loopback-guard :: public url host is 203.0.113.10
[PENDING] tls-certificate :: CheckOnly=true; no network access
[PENDING] http-health :: CheckOnly=true; no network access
[PENDING] mqtt-reachable :: CheckOnly=true; target=203.0.113.10:1883 not probed
VERDICT=BLOCKED :: 1 policy violation(s)
EXITCODE=1
```

### https 目标（静态检查通过，活检查 PENDING）

```
[PASS] target-present :: https://203.0.113.10
[PASS] https-scheme :: server target uses https
[PASS] loopback-guard :: public url host is 203.0.113.10
[PENDING] tls-certificate :: CheckOnly=true; no network access
[PENDING] http-health :: CheckOnly=true; no network access
[PENDING] mqtt-reachable :: CheckOnly=true; target=203.0.113.10:8883 not probed
VERDICT=PENDING :: 3 check(s) not executed; deployment unproven
EXITCODE=2
```

检查项：目标必须提供 / 服务器目标必须 https / 公网地址不得为 loopback /
TLS 证书有效期与 hostname 绑定 / 健康检查与 `Set-Cookie` 是否带 Secure / MQTT 地址可达性。
无目标或不可达一律 PENDING，绝不写成通过。

## 过程中修掉的缺陷

1. **`[CmdletBinding()]` 与 `param()` 之间插注释导致解析失败** —— 注释必须放在特性之前。
2. **行尾管道符换行** —— Windows PowerShell 5.1 不支持（PS7 才支持），需改为显式循环。
3. **中文注释 + PS 5.1 默认按 ANSI 读脚本** 的编码风险 —— 已按仓库既有风格改为纯 ASCII 英文。
4. **`git` 不可用时脚本直接崩溃** —— 改为检测命令存在性后报 PENDING，符合 fail-closed 契约。
5. **`Write-Output` 污染函数返回值** —— `Test-TargetPresent` 用 `return $true/$false` 判断，
   但函数内 Pend 的输出会一起进入返回值，导致"无目标"也被判为有目标并继续跑。
   改为脚本级标志 `$script:HasTarget`。
6. **`$Host` 不能作参数名** —— 是 PowerShell 内置变量。

## 仍未完成

1. `validate-deploy.ps1`：目标服务器 HTTPS/TLS、健康检查、`Set-Cookie` 是否带 Secure、
   MQTTS 端口可达性、公网 MQTT 地址解析。
2. `backup-restore.ps1`：备份与恢复后设备/模板/告警/遥测计数一致性校验。
3. 以上两项都需要真实目标环境才能产生证据，缺环境时按契约必须报 PENDING。

## 清理动作

- 验证用的空目录与输出文件均放在会话工作区，未写入本仓库。
