# P1.2 规则链版本（草稿/发布/回滚）—— 证据记录

日期：2026-09-11
环境：Windows / go1.26.2 windows-amd64

## 先更正一个此前误判

先前把 P1.2 记为「独立模块，工作量最大，还没动」。**这个判断是错的**——
它来自 ROADMAP.md 里 P1.2 没有实现状态小节，而不是代码本身。实际核查结果：

| P1.2 要求 | 实际情况 |
| --- | --- |
| 节点超时 | 已实现 `rule_chain_node_policy.go`（`timeout_ms`） |
| 指数退避重试 | 已实现（`max_attempts` / `backoff_ms`，且**默认禁止重试副作用节点**，需显式 `retry_safe=true`） |
| 失败分支 | 已实现（`rule_chain_failure_edge_test.go`，7 条用例） |
| 死信 | 已实现（终局失败下沉，可 `dead_letter=false` 显式退出，成功不产生死信） |
| 单消息 Trace | 已实现 `rule_chain_trace.go` / `rule_chain_trace_query.go` |
| 输入回放 | 已实现 `rule_chain_replay.go`（`ReplayRuleChainExecution`） |
| **草稿/发布版本与回滚** | **本轮补全**（语义层） |

规则链相关源文件 16 个，**既有用例 60 条全部通过**（`go test ./internal/service/ -run 'RuleChain|Rule_Chain'`）。

教训：ROADMAP.md 缺实现状态小节 ≠ 功能未实现。**判断前必须查代码，不能只看文档。**

## 本轮新增

文件：`backend/internal/service/rule_chain_version.go` + 同名测试。

语义要点：

- 版本沿 `draft -> published` **单向推进**；published 只读、不可重复发布。
  允许改已发布版本等于让线上正在跑的图被静默替换且无法追溯。
- **回滚产生新草稿，不回写原版本**（沿用本项目 OTA 批次回滚的既有约定
  `RollbackFleetCommandJob`），原历史保持只读，两侧各留一条审计事件。
- 版本号单调递增，由已有最大版本推导，**不接受外部传入**，避免跳号撞号。
- **图哈希参与版本身份**：内容未变不产生新版本，否则每次保存都堆一个空版本，
  把"改了什么"稀释掉。
- 非法流转（重复发布、归档后发布、回滚目标非 published、空标识）一律 fail closed。

## 实跑结果

```
go test ./internal/service/ -run 'PublishedVersionIsReadOnly|RepublishIsRejected|...' -count=1 -v
--- PASS: TestRuleChainPublishedVersionIsReadOnly (0.00s)
--- PASS: TestRuleChainRepublishIsRejected (0.00s)
--- PASS: TestRuleChainArchivedVersionCannotPublish (0.00s)
--- PASS: TestRuleChainDraftIsEditable (0.00s)
--- PASS: TestRuleChainSameGraphDoesNotCreateEmptyVersion (0.00s)
--- PASS: TestRuleChainNewDraftIncrementsVersion (0.00s)
--- PASS: TestRuleChainRollbackCreatesNewDraftAndKeepsHistory (0.00s)
--- PASS: TestRuleChainRollbackRejectsDraftTarget (0.00s)
--- PASS: TestRuleChainVersionRejectsMissingIdentity (0.00s)
ok      aetherlink-iot/backend/internal/service  0.300s
```

`gofmt -l` 对两个新文件返回空。

## 2026-09-12 追加：端点已接线（提交 1e2add7 + 接线提交）

迁移 93.sql 建 `rule_chain_versions` 表并登记三个新路由，随后完成 model/dal/service/api/router 接线：

```
GET  /api/v1/rule-chains/:id/versions       列出版本
POST /api/v1/rule-chains/:id/versions       新建草稿（图哈希未变不产生空版本）
POST /api/v1/rule-chains/versions/publish   发布（draft -> published，仅一次）
POST /api/v1/rule-chains/versions/rollback  回滚（造新 draft，不改写历史）
```

设计要点：

- 持久化编排**一律复用纯语义函数**，不另写一份状态机（避免两份规则漂移）。
  编排函数因此带 `Record` 后缀——**Go 没有重载**，与纯函数同名会直接编译失败。
- 发布用 DAL 带条件更新（`status='draft'`），影响行数 0 判为「已发布」并报错，
  **不静默成功**——并发重复发布静默成功等于让状态机形同虚设。
- 「一条链同时只能有一个 published」交给**数据库部分唯一索引**，
  不做应用层先查再写（高并发必漏判）。
- 租户一律取 `claims.TenantID`，不接受请求体指定。

### 实跑验证（全新空库）

```
MIGRATE_OK
sys_version            = 93
rule_chain_versions 表 = 已创建
casbin_rule 新增路径   = api/v1/rule-chains/:id/versions
                        api/v1/rule-chains/versions/publish
                        api/v1/rule-chains/versions/rollback
```

`gofmt` 合规；`model/dal/service/api/router` 单独编译通过；
规则链与版本语义用例 `ok`（1.321s）。

### 持久化行为的真实数据库验证（7 项全过）

用一次性程序（跑完即删，未提交）连真实 PostgreSQL 走完整流程：

```
OK create draft v1 (status=draft)
OK identical graph creates no empty version
OK publish v1
OK re-publish rejected
OK rollback -> v2 draft from v1, v1 still published
OK cross-tenant isolation (0 rows)
OK second published version rejected by DB constraint
ALL_CHECKS_PASSED
```

第 7 项的数据库报错原文，证明「一条链同时只能有一个 published」确实由数据库守住，
而不是应用层挡下来的：

```
ERROR: duplicate key value violates unique constraint
       "rule_chain_versions_single_published_idx" (SQLSTATE 23505)
```

覆盖：草稿创建、图哈希去重、发布、重复发布 fail closed、回滚造新草稿且原版本只读、
双向审计（2 条）、跨租户隔离、单 published 不变式。

## 边界与未完成

1. **仅语义层，未接持久化**。`model.RuleChain` 当前没有版本字段，
   落库需要新迁移；而迁移号位正被并行改动占用（已到 88），本轮不抢占。
2. **未接线 API/Casbin**：同 P0.5 的原因，新路由需登记资源表，否则后端 fail-fast 拒启动。
3. 未做多轮稳定性验证（单次 `-count=1`）。
4. 审计事件当前只在内存结构里产生，未落审计表。

按 ROADMAP §4，P1.2 因缺持久化与接线，**仍不得记为 done**；
本轮补的是"版本语义"这一最后缺口，且该部分有运行期证据。
