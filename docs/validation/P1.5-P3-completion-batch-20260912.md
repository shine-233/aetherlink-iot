# P1.5 / P1.6 / P2 / P3 补全批次证据（2026-09-12）

批次目标：把 2026-09-12 审计（`P1.5-P1.6-deadcode-audit.md`、`P2-P3-audit.md`）判定的
死代码与 pending 项补成真实接线。本批**全部代码与迁移已落地**，运行期验证受本机
环境限制（无网络→Go 模块缓存被今日磁盘清理清空且无法重装；无 Docker/真实 PostgreSQL；
系统盘剩余约 11 GB），按下文"已测/未测"双清单如实记录。

**环境事实（决定本批验证上限）**：

- `C:\Users\Zz\go\pkg\mod` 已被 2026-09-12 磁盘清理清空（仅剩 4KB 目录结构），
  proxy.golang.org / goproxy.cn / goproxy.io 均不可达，回收站无可恢复内容。
  `internal/service` 等重依赖包的**既有测试套件无法重跑**，本批新增的服务层测试同样只能以源码交付。
- 两个纯标准库叶子包（`pkg/license`、`pkg/pluginsdk`）**可以离线编译与测试**，
  本批拿到了真实运行证据（见各节）。
- 前端 node_modules 完整，vitest 全量套件可跑，本批前端改动有真实运行证据。

---

## P1.5 边缘节点注册/心跳/Reconcile（partial → 代码闭环）

### 交付物

- **迁移 `backend/sql/97.sql`**：`edge_nodes` 表（id=节点自报身份、tenant_id、version、
  capabilities JSON 数组、status active/revoked、last_seen_at 可空）+ 租户/心跳索引。
- **模型** `internal/model/edge_node.go`：`EdgeNode`、注册/心跳/Reconcile 请求与响应结构。
- **DAL** `internal/dal/edge_node.go`：
  `GetEdgeNodeByID`（**故意不带租户过滤**，供跨租户抢注判定）、`GetEdgeNodeInTenant`、
  `InsertEdgeNode`、`UpdateEdgeNodeRegistration`（条件更新 status='active'，revoked 不续命）、
  `TouchEdgeNodeHeartbeat`、`ListEdgeNodesInTenant`。
- **服务** `internal/service/edge_node_service.go`：
  - 注册：`EvaluateEdgeNodeRegistration`（既有决策层）→ 插入或条件更新；跨租户抢注拒绝。
  - 心跳：复用同一条注册判定（版本/能力可选携带）；返回 `ClassifyEdgeNodeHealth` 分类——
    **该函数从本文件起有真实数据来源**（此前零调用方）。
  - Reconcile：`ClassifyEdgeNodeHealth`（健康闸门）+ `CheckEdgeVersionCompatibility`
    （版本闸门，最低版本读 `edge.min_compatible_version`，未配置=一律不兼容 fail closed）
    + `PlanEdgeReconcile` 逐资源修订号比对（云端修订号从 `edge_sync_tasks` 历史 payload
    推导，复用 `edgeSyncPayloadRevision`）；计划里 `sync` 项经 `EdgeSyncService.CreateEdgeSync`
    真正下发（复用其冲突闸门）；存在 `needs_attention` 时整批停止自动下发。
- **API/路由** `internal/api/edge_node.go` + `router/apps/edge_sync.go`：
  - `POST /api/v1/edge/nodes`（注册）
  - `GET  /api/v1/edge/nodes`（列表+逐节点健康）
  - `POST /api/v1/edge/nodes/:node_id/heartbeat`
  - `POST /api/v1/edge/nodes/:node_id/reconcile`
- **Casbin**：97.sql 登记 g2 + SYS_ADMIN/TENANT_ADMIN 授权；
  `router/apps/casbin_route_registration_contract_test.go` 挂入 EdgeSync 组让契约测试持续守住。
- `pkg/global/global.go` VERSION_NUMBER 96 → 97。
- 修订号：`edge_sync.go` 的 `edgeSyncPayload.Revision` 与 `EdgeSyncRevisionFromHistory`
  已在基线提交中存在（本批消费，不重做）。

### 已测 / 未测

- 已测（历史证据，环境破坏后未复跑）：`edge_governance_test.go` 11 例、
  `edge_node_registry_test.go`、`edge_reconcile_test.go`、`edge_sync_revision_test.go`（决策层）。
- 未测（需环境）：四个新端点的 HTTP 路径、DAL 落库、Reconcile→CreateEdgeSync 真实下发、
  Casbin fail-fast 启动、真实边缘节点断云自治/重连同步/冲突人工确认。
- 静态核实（本批，grep+gofmt -e 语法）：全部接线点、零 import 死包删除安全性。

### 边界（如实记录，不宣称完成）

- 节点**证书**签发不在本批（复用 D5 X.509 模块为后续接线点，见 ROADMAP P1.5 交付物清单）。
- 真实边缘节点联调、断云自治演练需要活栈。

---

## P1.6 模板市场打包导入（partial → 代码闭环）

### 交付物

- **端点** `POST /api/v1/device/template/market/bundle/import`
  （`router/apps/device.go` + `internal/api/device_template_market.go`），
  与既有 `GET market/bundle` 出包端点同一载荷契约（导出的 bundle 原样回传即可）。
- **服务** `internal/service/device_template_market_import.go`，链路：
  `VerifyMarketBundle`（验签 fail closed）→ `CheckMarketBundleDependencies`
  （阻断项非空即拒）→ `PreviewMarketBundleImport`（只读预览）→
  覆盖项需显式 `confirm_overwrite=true` → 逐模板 `ImportDeviceTemplateWithTenant`
  （租户幂等）+ 逐模板 `EmitMarketTemplateImportAudit` 审计（created/idempotent/rejected 全留痕）。
  **此前三个零调用方的完整性函数从本文件起全部有真实入口。**
- 预览模式 `preview=true`：验签与依赖检查照常，不落库。
- 模型：`ImportMarketBundleReq/ImportMarketBundleRsp/MarketBundleTemplateImportResult`
  移入 `model/device_template_market.go`；`MarketBundleImportPreview` 移到 model 层
  （API 响应直接引用），service 保留类型别名，既有测试引用不变。
- DAL：`ListDeviceTemplateNamesInTenant`（覆盖判定用，去重成集合）。
- Casbin：97.sql 登记（SYS_ADMIN/TENANT_ADMIN）。

### 已测 / 未测

- 已测（历史证据，未复跑）：`device_template_market_integrity_test.go` 11 例
  （验签往返、篡改拒绝、依赖 6 场景、预览 3 场景）。
- 未测（需环境）：导入端点 HTTP 路径、真实坏包（坏签名/坏依赖）导入被拒、
  导入幂等、`confirm_overwrite` 人工闸门、审计日志落盘。

---

## P2.1 协议插件 SDK（pending → partial：SDK 契约落地）

### 决策：internal/roadmap 死包 → 删除

`git rm` 前置条件（审计文档要求"先把 EdgeNode 搬走"）已满足：
真实 `EdgeNode` 已落 `internal/model/edge_node.go`（P1.5）。
删除理由：① 零 import（两次独立 grep 复核）；② `Unwired*` 全部 `ErrNotImplemented`，
与 §5"不引入无消费方的新抽象"直接冲突；③ 与已接线的真实实现（注册决策层、
分析服务）签名分叉，两套并存是最大的假完成信号；④ **该包已提交，删除经 git 可完整恢复**。

### 交付物

- **`pkg/pluginsdk`**（叶子包，纯标准库，可独立分发）：
  - `ProtocolAdapter` 接口（ValidateConfig/Connect/Discover/ReadTelemetry/WriteCommand/
    Health/Close，全部带 ctx——与路线图草图的两处偏差在 doc 注释里说明理由）；
  - `Manifest` + `ParseManifest`/`Validate`：名称字符集、点分数字版本、transport 白名单、
    **宿主兼容判定**（`min_host_version` vs `HostVersion`，fail closed）、
    点表（重名拒绝、kind 白名单）、凭证字段（只声明不承载值）；
  - 配置 Schema 校验器（`schema.go`）：与 `internal/service/widget_schema.go` 同语义的
    JSON Schema 子集移植，**未知关键字编译期拒绝**。刻意不共享代码——SDK 必须脱离服务端编译。
- **接线（真实消费方）**：`PluginRegistryService.Create` 接受可选 `manifest` 字段，
  提供时必须通过 `pluginsdk.ParseManifest` 校验，不过即拒绝注册；原始 JSON 落
  `plugin_registries.manifest` 列（97.sql ALTER ... IF NOT EXISTS，可空，保持 D9 兼容）。
- 迁移：97.sql。

### 已测（真实运行证据，本批唯一可离线执行的后端测试）

```
GOTOOLCHAIN=local go test ./pkg/license/ ./pkg/pluginsdk/ -count=1
ok aetherlink-iot/backend/pkg/license    0.871s   （6 例 PASS）
ok aetherlink-iot/backend/pkg/pluginsdk  0.719s   （6 例 PASS）
GOTOOLCHAIN=local go build ./pkg/license/ ./pkg/pluginsdk/   # OK
GOTOOLCHAIN=local go vet   ./pkg/license/ ./pkg/pluginsdk/   # 无告警
```

pluginsdk 6 例：宿主兼容判定表、manifest 合法/8 类破坏性输入、schema 贯通
（含未知关键字拒绝）、JSON 解析路径、integer 拒绝非整数、`ProtocolAdapter` 编译期锚点
（stub 必须实现全接口——未来加方法是显式破坏性变更）。

### 未测 / 边界

- 注册端点的 manifest 字段 HTTP 路径（`internal/service` 依赖无法编译，测试以源码交付）。
- 无真实外部协议适配器（CAN/BACnet/BLE/LoRaWAN 按路线图属"按客户需求接入"）——
  SDK 契约与注册校验落地，**不宣称运行期插件可用**。

---

## P2.2 轻量分析（partial → 代码闭环）

### 交付物

- **基础异常检测**（此前为 0）：
  - 纯决策核心 `internal/service/telemetry_analysis_anomaly.go`：
    `bounds`（静态上下限）与 `deviation`（均值±Kσ，默认 K=3）两种规则；
    空序列/规则非法如实报错（"没数据"≠"无异常"）；σ=0 显式零命中。
  - 编排 `RunTelemetryAnomalyDetection`：复用分析服务的取数缝与设备权限缝，
    单设备失败不中断多设备检测。
  - 端点 `POST /api/v1/telemetry/analysis/anomaly`（router/apps/telemetry_analysis.go）
    + Casbin（97.sql，角色口径与 96.sql 既有分析路由一致：SYS_ADMIN/TENANT_ADMIN/TENANT_USER）。
  - 模型：`TelemetryAnomalyQuery/RuleSpec/Hit/DeviceResult/Result`（model/telemetry_analysis.go）。
- **仪表盘分享**：核查确认**已存在且完整**——`model.Board` 的
  `Published/PublishedAt/ShareToken` 列、`PublishBoard` 签发 token、
  公开路由 `GET /api/v1/board/shared/:token`（router_init.go:204，注册于 JWT 之前）。
  上一轮审计的"分享=0"结论已过时（分享能力在审计后、本批前的提交中落地）。
- **权限**：分析的逐设备访问复查（`ensureTelemetryDeviceReadAccess`）已在基线中存在。

### 已测 / 未测

- 已测（历史证据，未复跑）：`telemetry_analysis_core_test.go`、
  `telemetry_analysis_export_test.go`（聚合/对比/导出）。
- 未测（需环境）：anomaly 端点 HTTP 路径、`DetectSeriesAnomalies` 的规则表用例
  （以源码交付，未写独立测试文件——决策核心无外部依赖，环境恢复后补表驱动用例成本极低）。

---

## P2.3 数据保留与性能（partial：降采样+缓存落地；压测被环境阻塞）

### 交付物

- **降采样冷层**：
  - 迁移 97.sql：`telemetry_rollups` 表（PK 含 bucket_ms，1h 桶；min/max/avg/last/count）。
  - DAL `internal/dal/telemetry_rollups.go`：汇总 upsert（ON CONFLICT 覆盖，**作业可重跑**）、
    冷窗口聚合读取（**count 加权合并 avg**，min/max/count/last/sum 精确合并，输出与原始
    聚合路径同形的 x/y 行）。
  - 作业 `internal/service/telemetry_downsample.go`：`telemetry.downsample.enabled`
    门控（**默认关闭**，不改变既有部署行为）；仅直连数据库模式工作（外部 TSDB 显式跳过）；
    **不删原始数据**（冷层是加法，原始行清理仍归保留策略）；单目标失败不中断整批。
  - cron：每日 3 点（`initialize/croninit/cron.go`，紧随 2 点保留策略）。
  - **冷读消费方**：`RunTelemetryAnalysis` 的取数路径——整窗早于降采样边界时回落
    `telemetry_rollups`（`fetchTelemetryAnalysisSeries`，部分冷热窗口整体走原始路径，
    不跨层拼接）。rollup 表不是只写不读的死表。
- **查询缓存**：`telemetry_analysis_cache.go` —— 分析取数 TTL 缓存（进程内，
  `telemetry.analysis_cache.enabled` 门控默认关闭，TTL 默认 300s，4096 条护栏）。
  多实例各自回源（无外部依赖的取舍在注释里写明）。
- 配置占位：`configs/conf.example.yml` / `conf.yml` / `conf-dev.yml` 的 telemetry 段
  （并入**既有** telemetry 段，避免顶层键重复；`edge.min_compatible_version` 同批加入）。

### 已测 / 未测

- 未测（需环境）：rollup SQL 的真实执行（需要 PostgreSQL）、冷读路径、降采样 cron。
- **基准压测 / 容量模型：本机阻塞**——ROADMAP 门禁要求"压测不使用假数据掩盖数据库瓶颈"、
  需单实例+双实例报告；本机系统盘仅剩约 11 GB 且无网络，压测前须先释放空间
  （路线图 P2.3 状态小节已有同样警告）。**此项保持 pending，不在本批宣称。**

---

## P3 商业许可证边界（pending → partial：离线验证+两执法点落地）

### 交付物

- **`pkg/license`**（叶子包，纯标准库 Ed25519，完全离线）：
  `Document`（edition/features/max_devices/max_tenants/时间窗全在签名内）+
  `Material`（document 原始字节 + Ed25519 签名 + key_id）+ `Verifier`（多公钥轮换、
  逐级拒绝哨兵：malformed/unsigned/bad-sig/unknown-key/expired/not-yet-valid）。
  刻意不做硬件指纹（doc 注释说明理由）。
- **服务** `internal/service/license_service.go`：
  - `license.public_keys` 未配置 = 边界未启用，全部执法点放行（默认行为不变）；
  - 执法点 1（启动门控）：`license.required=true` 时 `main.go` 启动前必须持有效许可证；
  - 执法点 2（设备配额）：`max_devices>0` 且许可证有效时 `CreateDevice` 前置
    `enforceDeviceQuota`（`dal.CountAllDevices` 部署级计数）；
  - 状态查询：`GET /api/v1/license/status`（SYS_ADMIN；不返回材料，返回验证结论+SHA-256 摘要）。
- Casbin（97.sql，仅 SYS_ADMIN）+ 配置占位（三份 conf 的 license 段，默认全关）。

### 已测（真实运行证据）

```
GOTOOLCHAIN=local go test ./pkg/license/ -count=1 -v
ok aetherlink-iot/backend/pkg/license 0.871s —— 6 例全过：
  有效许可证往返、篡改（改配额不重签）拒绝、过期/未生效窗口、
  未受信密钥拒绝、坏公钥表拒绝、特性门 Allows 表
```

### 未测 / 边界

- 启动门控与配额执法的运行期路径（需可编译环境）；许可证签发工具（openssl 无关，
  可用任意 Ed25519 工具或后续补 CLI）未交付——验证侧先行，签发侧另列。

---

## 附：native-board-provider 项目增删改（前后端全链落地）

- **后端**：`board_projects` + `board_project_members`（一板一项目，
  `(tenant_id, board_id)` 唯一索引收口，**刻意不动 boards gen 模型**）；
  `internal/dal/board_project.go`、`internal/service/board_project.go`、
  `internal/api/board_project.go`、路由挂 `board/projects` 组（8 端点）；
  `GET /board` 列表支持 `project_id` 过滤（`none`=内置项目；成员 ID 集合下推 SQL）；
  `DeleteBoard` 前置清理归属（先删归属再删看板，失败即中止）。
- **前端**：`src/service/api/board.ts` 新增 8 个项目 API 封装；
  `native-board-provider.ts` 项目方法从 unsupported 改为真实现
  （内置项目恒为首项且不可改名/删除——fail closed 保留；创建看板后写归属；
  详情经 `member-of` 反查归属；列表 project_id 下推后端保证分页计数正确）；
  capabilities 更新为 create/update/delete: true。
- **已测（真实运行证据）**：
  - `npx vitest run src/service/visualization-provider` → **34/34 PASS**
    （其中 native-board-provider 13 例：项目列表/详情/增删改/内置项目不可变/
      看板归属与详情解析/既有看板 CRUD 回归）；
  - 全量套件 `npx vitest run` → **3755 例中 3750 过、5 失败**（失败明细见最终报告，
    均为存量用例与本批无关/待逐项核对，本批改动直接相关的 provider 目录全绿）。
- 未测：8 个新端点的运行期路径（后端无法编译）、`board/projects` 组的 Casbin fail-fast。

---

## 迁移 97.sql 汇总（可重跑，全部 IF NOT EXISTS / WHERE NOT EXISTS）

| 内容 | 对应项 |
|---|---|
| `edge_nodes` 表 + 索引 | P1.5 |
| `plugin_registries.manifest` 列 | P2.1 |
| `telemetry_rollups` 表 + 窗口索引 | P2.3 |
| `board_projects` / `board_project_members` 表 + 一板一项目唯一索引 | board provider |
| Casbin g2/p：edge/nodes×3、template/market/bundle/import、analysis/anomaly、license/status、board/projects×4 | 本批全部新路由 |

`pkg/global/global.go` VERSION_NUMBER = 97（与最大迁移编号一致）。

## 本批文件清单

**新增（后端）**：model/edge_node.go、model/telemetry_rollup.go、model/board_project.go、
dal/edge_node.go、dal/telemetry_rollups.go、dal/board_project.go、dal/device_count.go、
service/device_template_market_import.go、service/edge_node_service.go、
service/telemetry_analysis_anomaly.go、service/telemetry_downsample.go、
service/telemetry_analysis_cache.go、service/license_service.go、service/board_project.go、
api/edge_node.go、api/board_project.go、api/license.go、pkg/pluginsdk/{sdk,manifest,schema,sdk_test}.go、
pkg/license/{license,license_test}.go、sql/97.sql
**删除**：internal/roadmap/（6 文件，已提交可逆）
**新增（前端）**：无新文件（board.ts/provider 追加）
**修改**：见 git status（含三份 conf、cron、main.go 启动门控、ROADMAP.md 状态小节）

## 与统一验收门禁（§4）的对照

1. 源码交付：✅（迁移/DAL/service/API/前端/配置/文档齐）。
2. 契约测试：部分 ✅（license/pluginsdk/board-provider 有真实运行；服务层测试以源码交付）。
3. 运行证据：❌ 大部分（环境阻塞，如实标注 pending）。
4. 四面一致：❌（OpenAPI/前端 UI 未同步新端点——本批只有 provider 层前端；
   UI 页面接入是下一步）。
5. 状态标记：本批各项均为 **partial**（代码+接线+可达入口），无一宣称 done。

---

## 交叉复核附录（2026-09-12，应用户要求的多 agent 交叉核对）

方法：1 个只读审计 agent 做路线图全量清点、1 个 agent 做前端真实构建验证
（`pnpm typecheck` + vitest 子集 + 契约一致性）、本批接线由主会话逐链 grep 复核
（子 agent 通道受平台并发限制部分失败，改为直接核对，结论等价可复现）。

### 复核发现并已修复的缺陷（本批代码真实存在过两处编译错误）

1. **RotateToken 误注入**：接线 P2.1 时脚本替换未限次数，把 manifest 校验块同时注入了
   `PluginRegistryService.RotateToken`——该函数没有 `req` 变量，属编译错误。已移除，
   仅保留 `Create` 内的一处（plugin_registry.go:62-77）。
2. **model.Manifest 字段静默失配**：`PluginRegistry.Manifest` 列的模型编辑因空白差异
   未命中且脚本无断言，导致 service 侧 `Manifest: manifestSnapshot` 引用了不存在的字段。
   已补上（model/plugin_registry.go，带断言重做）。

教训已入流程：后续脚本化编辑一律带唯一性断言；"改了"必须以 grep 对账，不以脚本退出码为准。

### 复核确认的真实性（抽验全部通过）

- 接线链 11 组逐一核对：edge nodes×4 路由→EdgeNodeApi→EdgeNode 服务→dal→97.sql ✓；
  打包导入、anomaly、license/status（main.go:74 + device_create.go:27）、
  board/projects×8、P2.3 cron(每日3点)→fetchTelemetryAnalysisSeries(:114/:126)→rollup DAL、
  pluginsdk.ParseManifest(:65) 全部真实，依赖符号签名逐一比对无失配。
- Casbin：97.sql 覆盖全部新路径 + 角色；Board 组 17 条路径在 63/97.sql 全部有登记后
  已挂入契约测试（Device 组体量大，维持未挂载，列为遗留）。
- 前端：`pnpm typecheck` **0 错误通过**；定向 105 例 104 过；board.ts 8 个新函数与
  后端路由 8/8 对齐；thingsvis 内置项目守卫用导入常量（双保险：provider 层 fail closed）。
- 矛盾裁定：P0.3 进度回写"已回写"正确（87.sql progress 列 + UpdateFleetCommandJobDetailProgress
  + ConsumeFleetCommandJobProgress 端点均在），另一审计的"无代码"结论有误。
- design-token 失败（740>733）经 stash 实证 + 逐提交归因：adeaf80 +3、46f90e8 +4，
  均为 HEAD 存量，与本批无关。

### 全量清点结论（引用只读审计 agent 的覆盖率判断）

- 路线图交付物约 **75%**（38 项中 29 项）有生产接线代码，其中约 60% 达到
  "代码+调用方+定向测试"标准；
- 按 §4 四面一致门禁（OpenAPI/UI/E2E/运行证据）真正闭环的约 **25-30%**；
- **P3 的 12 个子项中 11 个零代码**（仅许可证边界落地）——多地域演练、计费、审计导出、
  自助开通、移动端商店、桌面运维、行业方案包、生态运营、第三方插件签名等全部未开垦；
- 基础设施类缺口：所有新端点未进 `backend/docs/openapi/openapi.json`（09-07 旧版）；
  edge/license/anomaly/bundle-import 无前端页面；automation_tests 无对应 E2E 用例；
- ROADMAP §1.2 中"native-board-provider 项目增删改仍 unsupported"等三处文档自述
  已落后于代码（本批已在 ROADMAP 补记更正）。

**因此对"是否全部覆盖"的如实回答是：代码覆盖约 75%，路线图按其自身验收门禁远未全部完成；
P3 长期能力区与四面一致的证据面是最大的真实缺口。**

---

## 迁移 94-97 真实 PostgreSQL 运行证据（2026-09-13 补）

环境：`C:\Users\Zz\al_pg_verify`（PostgreSQL 17，127.0.0.1:55432，trust）。
方法：克隆 93 版常驻验证库 → `CREATE DATABASE aetherlink_v97_test TEMPLATE aetherlink_v93_test`
（源库 `sys_version=93`，116 表）→ psql `-v ON_ERROR_STOP=1` 按序应用 94/95/96/97.sql。

| 步骤 | 结果 |
|---|---|
| 94.sql / 95.sql / 96.sql / 97.sql 依次应用 | **全部 exit=0，零报错** |
| 新对象存在性 | edge_nodes / telemetry_rollups / board_projects / board_project_members 四表 + plugin_registries.manifest 列全部存在 |
| Casbin g2 登记 | 10 条新路径全部入库（edge/nodes×3、bundle/import、analysis/anomaly、license/status、board/projects×4） |
| **负向对照①（一板一项目）** | 同一 board 插入第二个 project → `duplicate key ... board_project_members_board_uk` **正确拒绝** |
| **负向对照②（节点状态约束）** | `UPDATE edge_nodes SET status='bogus'` → `violates check constraint edge_nodes_status_check` **正确拒绝**；last_seen_at NULL 合法 |
| **幂等对照（97.sql 重跑）** | 全部 NOTICE skipping，exit=0，可重跑成立 |
| **rollup 汇总数学（真实数据）** | 种子 2 行（5.0、7.0 同一小时桶）→ min=5/max=7/**avg=6**/**last=7**（最新 ts 胜出）/count=2；`array_agg(... ORDER BY ts DESC)` 语法实跑通过 |
| **rollup upsert 幂等** | 第二次执行仍 1 行，ON CONFLICT DO UPDATE 生效 |

库 `aetherlink_v97_test` 保留作活证据（含种子数据与触发过约束的记录）。
sys_version 行为 93 属预期——版本行由 Go 迁移器（pg_init.go）写入，非 SQL 文件职责；
本证据覆盖的是 **SQL 层在真 PostgreSQL 上的可执行性、约束有效性与幂等性**。

仍未覆盖（需要 Go 工具链可用）：pg_init.go 对 1-97 全链的程序化迁移循环、
迁移后服务启动（casbin fail-fast 门禁）。

---

## 第二轮补充（2026-09-13）：P3 子项 + 前端页面 + OpenAPI 准备

### P3-a 第三方插件签名（已落地，真实测试）

`pkg/pluginsdk/signing.go`：Ed25519 非对称签名（厂商私钥签、平台公钥验——与 P1.6 的
HMAC 对称方案刻意区分：市场包是同一方签发验签，插件分发必须支持厂商持有私钥）。
签名覆盖除 Signature/SignedBy 外的规范 JSON；`SignManifest` 先 Validate 再签
（不给非法 manifest 盖章）；`VerifyManifestSignature` 按"未签名→未知密钥→签名不符"拒绝。
`Manifest` 增 Signature/SignedBy（omitempty，老 manifest 兼容）。
真实证据：`GOTOOLCHAIN=local go test ./pkg/pluginsdk/ -count=1` → **9/9 全过**
（往返、篡改、未知厂商、空受信表、非法 manifest 拒签、canonical 不含签名字段）。
注册侧接线语义：**未签名允许（D9 兼容过渡），带了签名就必须验过**（待后续接线到
PluginRegistry.Create 的受信厂商公钥表配置——本轮未做，见下）。

### P3-c 审计导出（已落地，代码+迁移）

- `POST /api/v1/operation_logs/export`（api/operation_log.go）+ `service/audit_export.go`
  + `dal.ListOperationLogsForExport` + 请求模型。
- 口径：当前租户（沿用既有列表查询的保守作用域，其注释明确要求跨租户须先补权限测试）；
  时间窗必填且 ≤1 年；行数上限 10 万；**request/response 载荷列刻意不导出**
  （审计最小化——导出文件脱离库的访问控制）。
- 迁移 **98.sql**（Casbin：g2 + SYS_ADMIN/TENANT_ADMIN/TENANT_USER，与列表 GET 同面），
  VERSION_NUMBER = 98。
- 未测：端点运行期路径（服务层无法离线编译）；98.sql 未在 PG 实例复跑（97 的流程
  已验证可复制）。

### P3-b max_tenants 租户配额（跳过，如实记录原因）

全仓无独立的"创建租户"服务/端点（租户经用户体系衍生）。给 `license.MaxTenants` 强行
找执法点会得到一个无消费方的死抽象（违反 §5）。留待租户自助开通（本身是 P3 未开垦项）
实现时一并接线。

### 前端页面（2/4 落地，typecheck 零错误）

- **license 状态页** `src/views/management/license/index.vue`：只读卡片（状态标签、
  版本、有效期、配额、特性、SHA-256 摘要、未启用态明示）+ `service/api/license.ts`。
- **边缘节点页** `src/views/management/edge-nodes/index.vue`：列表（健康标签/最近心跳/
  能力列）+ 幂等注册弹窗 + 心跳（NPopconfirm 确认）+ `service/api/edge-node.ts`
  （含 reconcile 封装，UI 暂不暴露编排入口）。
- elegant 路由手工补齐（`pnpm gen-route` 在本环境因 unicorn-magic×Node24 不兼容而失败，
  **存量问题**；生成文件手工补齐并逐字段对齐生成器形状：systemRoutes/imports/transform/
  typings 四处）+ 四语言 route/custom i18n 键。
- anomaly 检测与打包导入的 **UI 未做**（API 服务端已可用），如实列为剩余项。

### OpenAPI（注解已备，重生成待网络）

- 生成管线：`cd backend && go run ./cmd/openapigen -out docs/openapi/openapi.json`
  （路由枚举复用 RouterInit，需完整依赖图）——**离线不可行**（同测试阻塞）。
- 本批已为新端点补齐 swag 注解（@Summary/@Tags/@Router）：edge nodes×4、
  license/status、analysis/anomaly、market/bundle/import、board/projects×8、
  operation_logs/export——网络恢复后一条命令重生成。
- `swag.exe` 在 GOPATH/bin 中存在；注意生成器是自研 openapigen 而非 swag CLI 直出。

### 前端验证

- `pnpm typecheck` **0 错误**（含两个新页面 + 手工路由注册 + i18n）。
- 全量 vitest 终态见最终报告（唯一预期失败：design-token 存量）。

---

## 第三轮补充（2026-09-13）：把"可代码化缺口"干到底

### P2.1 厂商公钥验签接线（完成）

`PluginRegistry.Create`：manifest **带厂商签名就必须验过**（`plugin.trusted_vendor_keys`
配置 key_id → base64 ed25519 公钥；坏公钥条目报错而非跳过；未签名允许——D9 兼容过渡）。
至此插件签名的"签名→注册验签"链路闭环。

### P1.6 升级/回滚（完成，此前"零代码"的功能缺口）

- **迁移 99.sql**：`device_template_upgrade_history` 表（previous_payload 存旧版本完整
  导出载荷，即回滚凭据本身）+ 三条新路由 Casbin；VERSION_NUMBER=99。
- **服务** `service/device_template_upgrade.go`：
  - 升级 = 目标版本**严格新于**当前（点分数字逐段比较，降级必须走回滚通道）→
    捕获旧行完整导出载荷 → 经租户幂等导入新版本 → 落历史（历史落库失败如实报错，
    不伪造成功）；
  - **回滚 = 重放旧载荷，不是删行**（删行不可逆且牵连设备配置引用；重放幂等、
    版本共存、切换交给引用方）；
  - 历史 GET 端点供前端展示回滚点（TENANT_USER 可读）。
- 端点：`POST template/upgrade`、`POST template/upgrade/:history_id/rollback`、
  `GET template/upgrade/history`。

### 98/99.sql 运行期证据（待复跑，环境原因如实记录）

al_pg_verify 实例本日出现**持续性 0xC0000142 后端崩溃**（首个连接即触发，重启后复现；
与首轮 94-97 验证时同机可用状态不同，疑似磁盘 98% 满后的资源劣化）。98/99.sql 本轮
**未取得实跑证据**；两者 SQL 惯用法（CREATE TABLE IF NOT EXISTS / 部分索引 / INSERT
SELECT WHERE NOT EXISTS）与已在真 PG 实跑全过的 94-97 完全一致。复跑命令：
克隆库后 `psql -v ON_ERROR_STOP=1 -f 98.sql -f 99.sql`。

### 指标终态（①②两项的诚实上限）

- **①交付物接线代码**：38 项中 33 项（87%）。剩余 5 项全部**不是纯代码问题**：
  真实协议适配器（路线图自注"按客户需求接入 CAN/BACnet/BLE/LoRaWAN"）、
  移动端商店发布（客户端工程不存在）、桌面运维工具（独立产品）、
  HA 演练/RPO/RTO（实测动作）、行业方案包/生态运营（客户需求驱动）。
  硬写到 100% 只能靠假完成——正是本项目审计文化明令禁止的。
- **②代码+调用方+定向测试**：服务层定向测试随各功能以源码交付
  （升级/回滚、签名验签、审计导出），**待网络恢复即可运行**；
  两个叶子包已真实运行（license 6/6、pluginsdk 9/9）。
