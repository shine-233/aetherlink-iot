# Hooks Workspace Package

`frontend/packages/hooks` contains reusable Vue hooks shared across frontend modules.

## Review Notes

Shared hooks should keep side effects explicit and remain generic enough for multiple consumers.
