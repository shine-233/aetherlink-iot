# Utils Workspace Package

`frontend/packages/utils` contains reusable frontend utility functions.

## Review Notes

Keep package utilities generic, typed, and covered by focused tests when behavior is shared across features.
