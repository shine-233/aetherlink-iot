/**
 * 文件用途：统一导出 nanoid ID 生成能力。
 * 核心逻辑：从 nanoid 包转发同名函数，供内部包使用统一来源。
 * 关键注意事项：这是薄转发模块，升级 nanoid 版本时需要关注输出格式和运行环境兼容性。
 * 重构建议：如项目需要固定长度或前缀 ID，可新增项目级包装函数而不是修改原导出语义。
 */
import { nanoid } from 'nanoid'

export { nanoid }
