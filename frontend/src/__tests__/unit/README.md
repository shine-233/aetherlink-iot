# 前端单元测试目录

## 用途

这里存放前端 Vitest 单元测试，当前主要覆盖 ThingsVis 嵌入、鉴权、空间解析等工具逻辑。

## 命名约定

- 测试文件使用 `*.test.ts` 后缀。
- 文件名应对应被测能力或模块，例如 `thingsvis-auth.test.ts`。
- 用例标题应描述业务行为，而不是只描述实现细节。

## 维护注意事项

- 新增测试要有明确断言，不能只验证函数被调用。
- 涉及全局对象、网络请求或本地存储时要在用例间清理 mock。
- 修改运行时契约后同步补充正向、异常和兼容输入测试。
