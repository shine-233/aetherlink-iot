# 设备卡片展示组件

## 目录定位

本目录属于 AetherLink IoT 前端的 设备卡片展示组件 范围，用于集中维护该能力的源码、测试或静态资产。目录内文件应保持职责清晰，避免把跨领域逻辑直接写入局部组件。

## 主要文件/资产

- `index.vue`: 本目录内的主要文件或资产，需要与 设备卡片展示组件 的职责保持一致。

## 依赖关系

依赖 Vue 组件运行时、Naive UI、项目别名 `@/`、局部 store/service/composable；组件入参和事件是外部页面的主要契约。

## 审查发现

审查发现主要集中在目录职责边界、外部契约和回归验证是否清晰；本次只补文档，不改变运行逻辑。

## 重构建议

建议把复杂数据准备、接口调用和展示状态拆到 composable 或纯函数，组件保留清晰的输入输出。

## 验证建议

优先运行本目录 targeted ESLint；涉及交互或状态时再补对应 Vitest，不需要 broad build/test。
