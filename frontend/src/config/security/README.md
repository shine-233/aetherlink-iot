# 安全配置目录

## 用途

这里维护前端安全能力相关的配置、类型和导出入口，当前主要覆盖 RSA 公钥、算法参数和环境变量覆盖逻辑。

## 命名约定

- `index.ts` 负责对外导出安全配置能力。
- `rsa.ts` 放置 RSA 默认公钥、算法参数和校验函数。
- `types.ts` 只放类型定义，避免引入运行时代码。

## 维护注意事项

- 公钥更新前要确认后端加密/解密链路同步更新，避免登录或敏感请求加密失败。
- 环境变量覆盖逻辑需要保持显式，避免生产环境误用测试公钥。
- 如果新增算法或密钥来源，请同时补充类型、默认配置和必要测试说明。
