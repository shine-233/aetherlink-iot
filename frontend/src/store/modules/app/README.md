# 应用全局状态模块

## 目录定位

本目录属于 AetherLink IoT 前端的 应用全局状态模块 范围，用于集中维护该能力的源码、测试或静态资产。目录内文件应保持职责清晰，避免把跨领域逻辑直接写入局部组件。

## 主要文件/资产

- `index.ts`: 本目录内的主要文件或资产，需要与 应用全局状态模块 的职责保持一致。

## 依赖关系

依赖 Pinia、枚举常量、本地存储和相关 service；状态字段会被页面、路由守卫和测试共享。

## 审查发现

审查发现主要集中在目录职责边界、外部契约和回归验证是否清晰；本次只补文档，不改变运行逻辑。

## 重构建议

建议继续把副作用和状态推导拆分，给持久化、权限和跨模块联动补 focused 单测。

## 验证建议

优先运行本目录 targeted ESLint；涉及交互或状态时再补对应 Vitest，不需要 broad build/test。
