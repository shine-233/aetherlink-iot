# 通知组页面测试说明

本目录覆盖通知组列表页的高频业务路径，重点验证初始化拉取、表格渲染、启停、删除、新增编辑入口和分页刷新。

## 测试覆盖目标

- `index.test.ts` 覆盖列表加载、空数据分支、操作列、启停切换、删除确认、编辑回填、新增入口和分页参数。

## 维护规则

- 调整通知组 API 响应结构、分页字段或操作按钮时，需同步更新列表数据工厂和列断言。
- 不要在本目录引入真实网络请求，所有接口应通过 mock 保持可重复执行。

## 已知缺口

- 尚未覆盖真实权限控制、通知成员详情的端到端编辑链路和后端错误码细分提示。
