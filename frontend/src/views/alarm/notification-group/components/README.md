# 告警通知组组件

`components` 存放通知组编辑弹窗和成员数据组件。

## 目录职责

- `table-action-modal.vue` 管理通知组新增编辑表单。
- `member-type-data.vue` 管理不同成员类型的查询、选择和删除。
- `__tests__/` 覆盖成员滚动、搜索和弹窗提交。

## 维护注意

- 成员列表存在分页和搜索状态，避免在多个组件中重复维护同一份状态。
- 通知方式选项需要与业务常量和后端接口保持一致。