# 通知记录测试说明

本目录覆盖通知发送历史列表的查询与日期筛选合同，保证用户能按条件查看通知结果。

## 测试覆盖目标

- `index.test.ts` 覆盖页面挂载拉取、搜索参数、重置搜索、日期范围选择和清空日期范围。

## 维护规则

- 修改时间字段、搜索字段或分页字段时，需同步更新查询参数断言。
- 保持接口 mock 与页面查询模型一一对应，避免只断言渲染不验证请求参数。

## 已知缺口

- 尚未覆盖通知发送状态颜色、失败原因详情和跨时区日期格式展示。
