# RDI 告警概览测试说明

本目录覆盖告警域复用 RDI 概览入口的包装合同，确认该路由正确承载仪表盘 RDI 概览组件。

## 测试覆盖目标

- `index.test.ts` 覆盖子组件挂载、根结构、生命周期、多实例和浅挂载场景。

## 维护规则

- 如果告警域新增自己的筛选、跳转或告警态包装，需在本目录补充对应断言。
- 当前测试只验证包装关系，不应把 dashboard 原组件的内部行为重复搬到这里。

## 已知缺口

- 尚未覆盖告警域专属筛选、RDI 数据异常态和从告警记录跳转概览的真实链路。
