# 告警消息页面测试说明

本目录覆盖告警消息总入口的标签页与子组件接线，保证配置页和历史页在同一入口下稳定呈现。

## 测试覆盖目标

- `warning-message.test.ts` 覆盖根容器、告警中心标题、两个标签页和对应子组件挂载关系。

## 维护规则

- 新增告警子页或调整标签顺序时，需要同步更新标签页断言和 README。
- 保持测试聚焦入口装配，具体业务逻辑放到 `components/__tests__`。

## 已知缺口

- 尚未覆盖标签切换后的异步加载、权限隐藏和移动端布局。
