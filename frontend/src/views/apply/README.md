# 应用接入视图

`frontend/src/views/apply` 管理协议服务与插件接入，是外部服务纳入平台的前端入口。

## 目录职责

- `service/` 管理协议插件列表、新增编辑弹窗和删除流程。
- `plugin/` 管理服务注册、配置弹窗和协议配置表单。
- `__tests__/` 验证服务与插件路由契约，子目录测试覆盖列表和弹窗交互。

## 维护注意

- 服务和插件页面依赖不同 API 命名空间，修改时不要混用 service 与 plugin 接口。
- 表单字段直接影响接入配置，新增字段需要同步接口契约、国际化文案和测试数据。