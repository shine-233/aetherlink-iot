# 应用接入路由测试说明

本目录覆盖应用接入的路由级产品面，确认插件市场和协议服务属于应用接入业务能力，而不是普通组件演示。

## 测试覆盖目标

- `service-and-plugin-flows.test.ts` 覆盖 apply 下 service/plugin 路由注册和业务分层边界。

## 维护规则

- 新增应用接入路由时，应在这里补充路由归属说明，避免遗漏 P1 产品面。
- 本目录只放跨 service/plugin 的路由级测试，具体页面行为放到各子目录。

## 已知缺口

- 尚未覆盖真实导航菜单可见性、权限拦截和从路由进入页面后的完整用户流程。
