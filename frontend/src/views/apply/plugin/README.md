# 插件接入管理

`plugin` 目录负责服务注册列表、插件新增编辑和接入配置。

## 目录职责

- `index.vue` 管理服务列表、状态展示、删除确认和配置弹窗。
- `components/` 提供插件表单、注册弹窗和配置弹窗。
- `__tests__/` 覆盖页面契约、表单渲染和配置提交。

## 维护注意

- 插件注册与插件配置接口职责不同，改动时需保持弹窗入口和提交 API 对应。
- 当前存在业务展示字符串的历史编码问题，若要修复需单独评估 UI 文案和测试断言。