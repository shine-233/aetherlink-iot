# 插件接入组件

`components` 存放插件接入页面的表单与弹窗组件。

## 目录职责

- `form.vue` 管理协议配置表单片段。
- `serviceModal.vue` 处理插件注册新增/编辑。
- `serviceConfigModal.vue` 处理已有服务配置更新。

## 维护注意

- 组件通过 `getList` 事件通知父页面刷新，不应在组件内直接承担列表状态。
- 配置字段与协议类型强相关，新增协议时需要补齐分支测试。