# 协议服务管理

`service` 目录负责协议插件的列表、分页、新增、编辑和删除。

## 目录职责

- `index.vue` 编排列表查询、分页状态、删除确认和弹窗打开流程。
- `components/table-action-modal.vue` 封装协议服务新增/编辑表单。
- `__tests__/` 覆盖页面列表流程和弹窗提交契约。

## 维护注意

- 表单提交前需保持必填校验与新增/编辑模式区分。
- 删除和刷新列表应保持用户可见反馈，避免静默失败。