# 协议服务组件测试说明

本目录覆盖协议服务列表弹窗组件，保证新增编辑协议插件时的字段、标题和表单状态稳定。

## 测试覆盖目标

- `table-action-modal.test.ts` 覆盖新增/编辑标题、设备选项、附加信息、表单默认值和编辑回填。

## 维护规则

- 新增协议服务字段或设备类型时，需同步表单默认值、选项和提交断言。
- 保持弹窗测试隔离在组件层，不直接验证父页面刷新逻辑。

## 已知缺口

- 尚未覆盖协议字段远端校验、附加信息复杂格式和保存失败提示。
