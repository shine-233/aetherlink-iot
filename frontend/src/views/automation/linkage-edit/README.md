# 联动规则编辑页说明

## 目录职责

该目录负责自动化联动规则的新建与编辑，组织“如果满足这些条件，则执行这些动作”的整页表单。

## 关键文件

- `index.vue`：页面入口，处理路由参数、详情回显、提交映射和保存后返回。
- `modules/edit-premise.vue`：条件编辑子表单。
- `modules/edit-action.vue`：动作编辑子表单。

## 维护提示

- 页面内最容易出错的是“接口结构 <-> 前端表单态”的双向映射。
- 修改条件或动作结构时，要同时检查新建提交流程和编辑回显流程。
