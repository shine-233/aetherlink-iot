# API 密钥管理

`api` 目录负责 API Key 的列表、启停、新增编辑和复制反馈。

## 目录职责

- `index.vue` 编排 API Key 表格、分页、启停、删除和复制提示。
- `modules/table-action-modal.vue` 提供新增编辑弹窗。
- `__tests__/` 覆盖列表加载、操作按钮和弹窗提交流程。

## 维护注意

- 复制能力依赖浏览器安全上下文，改动时保留失败提示分支。
- Key 状态变更属于权限敏感操作，需要保持确认和刷新逻辑一致。