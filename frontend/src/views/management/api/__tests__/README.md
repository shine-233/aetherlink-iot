# API Key 页面测试说明

本目录覆盖 API Key 管理页的列表和高频操作入口，保证 API Key 能加载、新增、编辑、删除和启停。

## 测试覆盖目标

- `index.test.ts` 覆盖列表初始化、数据填充、空态、添加、编辑、删除、启停和分页相关状态。

## 维护规则

- 修改 API Key 响应结构、启停字段或操作列时，需同步更新接口 mock 和断言。
- 敏感字段不要在测试快照或 README 中写入真实密钥。

## 已知缺口

- 尚未覆盖真实密钥脱敏展示、复制行为、租户隔离和后端错误码提示。
