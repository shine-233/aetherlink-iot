# API Key 弹窗测试说明

本目录覆盖 API Key 管理的新增编辑弹窗，确保租户、名称和密钥相关表单合同稳定。

## 测试覆盖目标

- `table-action-modal.test.ts` 覆盖弹窗标题、显隐双向绑定、默认模型、租户校验和关闭事件。

## 维护规则

- 修改 API Key 字段、租户选择或校验规则时，需同步更新表单模型和规则断言。
- 本目录只测弹窗组件，列表刷新和启停逻辑放在父页面测试中。

## 已知缺口

- 尚未覆盖密钥生成/展示安全、租户权限边界和保存失败提示。
