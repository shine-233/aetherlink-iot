# 权限元素管理

`auth` 目录负责权限元素列表与新增编辑。

## 目录职责

- `index.vue` 展示权限元素表格、分页和删除确认。
- `components/table-action-modal.vue` 管理权限元素新增编辑表单。
- `__tests__/` 覆盖列表与弹窗交互。

## 维护注意

- 权限元素和路由权限强相关，新增字段时同步业务常量和接口契约。
- 删除权限元素可能影响角色授权，页面需要保留明确确认和反馈。