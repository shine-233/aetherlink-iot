# 权限元素弹窗测试说明

本目录覆盖权限管理的新增编辑弹窗，确保菜单/元素表单、标题和显隐事件稳定。

## 测试覆盖目标

- `table-action-modal.test.ts` 覆盖表单模型、校验规则、标题计算、显隐双向绑定和关闭事件。

## 维护规则

- 修改权限元素字段、父子关系或校验规则时，需同步更新表单断言。
- 弹窗测试不直接验证列表刷新，父页面负责接口和表格状态。

## 已知缺口

- 尚未覆盖权限编码唯一性、父级权限加载失败和保存失败后的用户提示。
