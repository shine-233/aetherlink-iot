# 通知服务管理

`notification` 目录管理邮件、短信和推送通知服务配置。

## 目录职责

- `index.vue` 组织三个配置页签或配置组件。
- `components/` 存放邮件、短信和推送通知配置表单。
- `__tests__/` 覆盖服务配置加载、编辑和测试发送反馈。

## 维护注意

- 通知服务配置通常包含敏感连接参数，前端只保留必要展示和校验。
- 发送测试消息时需保留加载态和失败态，避免误判配置已生效。