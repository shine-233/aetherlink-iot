# 通知配置页面测试说明

本目录覆盖系统通知配置总入口，确认邮件、短信和推送三个配置组件按标签页挂载。

## 测试覆盖目标

- `index.test.ts` 覆盖卡片壳、标签页顺序、翻译 key 和子组件接线。

## 维护规则

- 新增通知渠道或调整标签顺序时，需要同步更新标签页断言。
- 具体渠道的加载和保存逻辑应放在 `components/__tests__`。

## 已知缺口

- 尚未覆盖渠道权限隐藏、标签切换懒加载和配置保存后的跨标签刷新。
