# 角色管理

`role` 目录负责系统管理域下的角色列表、权限配置和辅助弹窗。

## 目录职责

- `index.vue` 管理角色表格、分页、删除和弹窗入口。
- `modules/` 提供角色新增编辑、权限编辑和列设置。
- `__tests__/` 覆盖角色列表与各模块交互。

## 维护注意

- 角色权限树默认包含首页权限，修改时需同步权限树测试。
- 角色操作属于权限敏感路径，失败态和刷新时机不能省略。
