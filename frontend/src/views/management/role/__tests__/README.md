# 后台角色页面测试说明

本目录覆盖后台角色管理页的列表、高频操作入口和权限相关弹窗接线。

## 测试覆盖目标

- `index.test.ts` 覆盖角色列表加载、添加、编辑、删除、启停、权限编辑入口、密码编辑入口和分页状态。

## 维护规则

- 修改角色 API、操作列、权限入口或密码入口时，需同步更新 mock 数据和断言。
- 本目录覆盖当前主用的后台角色管理页，包含权限和列设置相关能力，测试不要退回到已移除的旧路由壳模型。

## 已知缺口

- 尚未覆盖真实权限保存结果、角色启停失败提示和跨租户权限边界。
