# 角色管理模块

`modules` 存放角色管理页面的弹窗和列设置组件。

## 目录职责

- `table-action-modal.vue` 负责角色新增编辑。
- `edit-permission-modal.vue` 负责权限树加载、默认选中和授权提交。
- `column-setting.vue` 提供局部列设置辅助交互。

## 维护注意

- 权限树字段与后端元素结构绑定，变更时同步转换函数和测试。
- 列设置仅影响当前页面展示，不应改变服务端角色数据。
