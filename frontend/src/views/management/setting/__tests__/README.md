# 系统设置页面测试说明

本目录覆盖系统设置总入口，确保各设置面板按预期标签顺序呈现并可切换。

## 测试覆盖目标

- `index.test.ts` 覆盖默认数据清理面板、全部设置标签、翻译 key、子组件挂载和标签切换。

## 维护规则

- 新增、删除或重排设置面板时，需同步更新标签顺序断言。
- 具体设置项的接口与表单逻辑应放在 `components/__tests__`。

## 已知缺口

- 尚未覆盖权限隐藏、路由 query 指定默认 tab 和设置保存后的跨面板刷新。
