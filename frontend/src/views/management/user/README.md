# 后台用户管理

`user` 目录负责系统管理域下的用户列表、资料、密码和区域信息维护。

## 目录职责

- `index.vue` 管理用户表格、分页、启停状态和操作入口。
- `components/` 提供新增编辑、改密和列设置组件。
- `__tests__/` 覆盖列表加载、弹窗表单和列设置交互。

## 维护注意

- 用户区域、时区、手机号和角色字段需要与后端枚举和校验保持一致。
- 历史业务展示字符串中存在编码问题，如需修复需单独同步 UI 文案与测试断言。