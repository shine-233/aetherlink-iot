# 后台用户页面测试说明

本目录覆盖后台用户管理页的列表、状态映射、分页和高频操作入口。

## 测试覆盖目标

- `index.test.ts` 覆盖用户列表加载、空态、分页、状态选项翻译、新增、编辑、删除、启停和密码编辑入口。

## 维护规则

- 修改用户 API、状态枚举、操作列或分页字段时，需同步更新接口 mock 和断言。
- 本目录覆盖当前主用的后台用户管理页，包含列设置、区域和密码相关能力，不要退回到已移除的旧路由壳模型。

## 已知缺口

- 尚未覆盖真实区域权限、批量操作、密码保存失败提示和租户隔离。
