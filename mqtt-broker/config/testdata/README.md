# Config Testdata

`mqtt-broker/config/testdata` contains sample configuration files used by config tests.

## Review Notes

Do not store real credentials here. Keep samples minimal and aligned with config parser tests.
