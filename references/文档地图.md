# 文档地图

生成日期：2026-07-27，**整理日期：2026-07-30**。用途：定位本项目的客户需求、进度、工程参考与验证证据文档。

**绝对权威**：[`客户需求主清单-进度总表.md`](./客户需求主清单-进度总表.md)。REQ-01 ~ REQ-58 全部状态、待客户确认清单和能力边界以主清单为准。

## 0. 2026-07-28 文件整理说明（先读这条）

已按"只留当前需要的，其余清掉"整理完毕：

- 历史进度快照、逐轮 ledger、已完成的计划文档（共 31 份）先移入存档，随后**按用户指示于 2026-07-28 删除**。包括：根目录 `PROGRESS_A/C/F/G.md`、本地 Dev 会话记录、原 `audit_reports/` 全部追溯文档（迁移 ledger、需求追踪矩阵、缺口计划、修复重构计划）、`references/` 下的历史快照与分支 ledger。
- 其中 19 份可从 git commit `30cbd48` 恢复；**另外 11 份（4 个 PROGRESS_* 与 7 份 audit_reports 文档）从未进入 git，已永久删除**，其结论仅存于主清单。
- 下文按角色列出仍保留的文档；“保留”不等于并列权威，客户需求、进度、待确认项和能力边界仍只以主清单为准。

## 一、当前入口（Canonical）

| 文档 | 定位 | 一句话说明 |
|---|---|---|
| `references/客户需求主清单-进度总表.md` | 需求 + 进度总入口 | 单一权威表，覆盖 REQ-01 ~ REQ-58、10 处默认拍板、可做/需环境边界 |
| `references/文档地图.md`（本文件） | 文档定位索引 | 保留文档的角色说明 + 整理记录 |

迁移 ledger 与 P0/P1 路线图原在 `audit_reports/`，现已删除；**迁移编号口径改以主清单的"全局真相"小节为准**。原需求追踪矩阵和缺口计划不再是现行入口。

## 二、根目录说明文档（当前入口，非需求类）

这些文档被 `deploy/package.sh` / `package.ps1` 打包或被 `CLAUDE.md` 点名，**不可移动**。

| 文档 | 定位 |
|---|---|
| `CLAUDE.md` | 项目配置与工作规则、权威文档索引、迁移编号规则 |
| `README.md` | 项目概述、目录结构（打包引用） |
| `START-HERE.md` | 傻瓜式部署入口（打包引用） |
| `VALIDATION.md` | 验证门槛与当前证据边界（打包引用） |
| `SECURITY.md` | 安全策略、本地密钥清单（打包引用） |
| `THIRD_PARTY_NOTICES.md` | 第三方 license 声明（打包引用） |
| `PUBLICATION.md` | 公开源码范围与本地材料忽略清单 |
| `COMPATIBILITY.md` | broker plugin / ThingsVis / telemetry gRPC 外部合约 |
| `GENERATED_FILES.md` | 生成文件保留策略 |
| `SUPPLY_CHAIN.md` | 本地依赖输入门禁与可选外部供应链能力边界 |
| `CONTRIBUTING.md` | 模块边界与提交规则 |

## 三、references 保留文档（仍有效的工程参考）

这些文档承担各自的工程规则或接口说明，不承担客户需求或整体进度叙述；相关表述与主清单冲突时，以主清单为准。

| 文档 | 定位 |
|---|---|
| `README.md` | 目录说明与使用规则 |
| `dependency-capability-matrix.md` | 核心、本地替代、生成依赖和外部可选能力矩阵 |
| `developer-guide.md` | 开发入口、兼容边界与轻量检查指引 |
| `api-guide.md` | Command Job / Direct Method 等 API 合同 |
| `plugin-guide.md` | 前端插件管理与 Broker 插件边界 |
| `plugin-runtime-surface.md` | broker aetherlink 插件文件/hook 目录 |
| `coverage-criteria.md` | 覆盖分层与弱断言规则 |
| `mcp-integration-guide.md` | MCP 集成建议（design-only） |
| `mcp-tool-contract.md` | MCP 工具合同草案（design-only） |

### 被自动化测试读取，务必留在原位

| 文档 | 读取方 |
|---|---|
| `source-quality-review.md` | `automation_tests/lib/coverage_contract.js`、`tests/00_coverage_contract.test.js` |

公开 clone 只保留当前源码质量审阅清单；内部历史盘点快照不属于公开测试输入。

### 历史快照与兼容短状态（非权威入口）

以下文件仅为旧链接兼容、专项边界说明或历史回溯保留；不得据此覆盖主清单中的客户需求、进度、待确认项或能力边界。

| 文档 | 定位 |
|---|---|
| `current-baseline.md` | 历史源码切片基线；不代表当前客户需求或进度 |
| `live-short-status.md` | 兼容短状态与逐轮快照；不作为当前进度依据 |
| `coverage-short-status.md` | 覆盖专项兼容短状态；只描述 coverage/harness 边界 |
| `customer-feature-short-status.md` | 客户可见功能的历史静态快照；不作为需求完成依据 |

### 运行证据

| 目录 | 定位 |
|---|---|
| `verification/` | 运行、API、E2E 等验证产物的证据目录；不承担客户需求或进度叙述 |

## 四、客户原件与翻译核对（只读，不进存档）

原始 docx 是客户交付物，**只读不修改**。结论已并入主清单 A 节（REQ-22）与 F 节。

| 文件 | 定位 |
|---|---|
| `客户的新需求/Parlek - 主要开发工作清单.docx` | 客户原件 |
| `客户的新需求/用户新增需求.docx` | 客户原件（含 24 张截图） |
| `客户的新需求/交互平台待改进方面.docx` | 客户原件（含 6 张截图） |
| `客户的新需求/_translation_check/screenshot_translation_findings.md` | 逐张截图中文原文与建议英文翻译 |
| `客户的新需求/_translation_check/**/document_text.json` | 段落文本抽取 |

## 五、存档（不再碰、不再看）

历史进度快照与原 `audit_reports/` 追溯文档已于 2026-07-28 **删除**，不再存在于任何目录；下表只列仍保留的客户原件与方案。

| 文件 | 内容 |
|---|---|
| `存档/RDI监控平台改造方案_最终版 copy.md` | 乙方方案，REQ-27 ~ REQ-58 原文来源 |
| `存档/RDI原文一一对应复核报告_20260619.md` | 旧站 / 方案 / 手册逐条原文对应 |
| `存档/Thinspanel全项目问题清单与错误修复重构计划_2026年06月19日.md` | 6 月 19 日历史问题清单 |
| `存档/RDI控制器和监控平台用户手册250918.docx` | 客户手册原件，只读 |

## 六、维护规则

1. 需求/进度结论以主清单为唯一入口。存档文档不再单独宣布"完成/发布就绪"。
2. 新增文档同步登记到本地图；文档失效时直接删除并在本地图注明，不在原位留空壳。
3. 迁移编号与证据边界以主清单"全局真相"小节为准。
4. `verification/` 仅保留运行时证据，不承担需求叙述。
